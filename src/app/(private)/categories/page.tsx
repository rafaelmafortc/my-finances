import PrivatePageLayout from '@/components/page-layout';

export default function Categories() {
    return (
        <PrivatePageLayout title="Categorias">
            <div className="w-full h-full border-2 border-dashed rounded-lg p-2 border-accent">
                Categories
            </div>
        </PrivatePageLayout>
    );
}
