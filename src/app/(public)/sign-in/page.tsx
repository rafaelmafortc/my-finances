import { ThemeSwitcher } from '@/components/theme-switcher';

export default function SignIn() {
    return (
        <main>
            <div>Sign In</div>
            <ThemeSwitcher />
        </main>
    );
}
